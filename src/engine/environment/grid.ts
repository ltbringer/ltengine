import { Position } from "../../engine/position";
import { Shape } from "../../engine/entity/shape";

class Grid2D {
    rows: number;
    cols: number;
    constructor(rows: number, cols: number) {
        this.rows = rows;
        this.cols = cols;
    }

    insert(pos: Position, shape) {

    }
}