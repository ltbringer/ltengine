import { Position2D } from "../../base/position2D";
import { Shape2D } from "../../base/shape2D";

export class Grid2D {
    rows: number;
    cols: number;
    constructor(rows: number, cols: number) {
        this.rows = rows;
        this.cols = cols;
    }

    insert(pos: Position2D, shape: Shape2D) {

    }
}